#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 *  main - Entry point
 *
 *  Return: Always 0 (Success)
 */
int main(void)
{
	int w = 48;
	int al = 'a';

	while (w <= 57)
	{
		putchar(w);
		n += 1;
	}
	while (al <= 'f')
	{
		putchar(al);
		al++;
	}
	putchar('\n');

	return (0);
}
